name: CI
on:
  push:
    branches: [main]

jobs:
  build-deploy:
    name: docs
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@master

      - name: 安装 Node
        users: actions/setup-node@v1
        with:
          node-version: 14.18.3

      - name: 安装依赖
        run: npm install

      - name: 打包
        run: npm run build

      - name: 发布
        uses: easingthemes/ssh-deploy@2.2.1
        env:
          ACCESS_TOKEN: ${{ secrets.PRIVATE_KEY }}
          ARGS: '-avzr --delete'
          SOURCE: '../../docs/.vitepress/dist/'
          REMOTE_HOST: ${{ SECRETS.REMOTE_HOST }}
          REMOTE_USER: 'root'
          TARGET: '/www/wwwroot/lznote/'
